import { PlanTransformPipe } from './plan-transform.pipe';

describe('PlanTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
