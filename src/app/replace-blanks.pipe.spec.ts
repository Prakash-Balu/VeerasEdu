import { ReplaceBlanksPipe } from './replace-blanks.pipe';

describe('ReplaceBlanksPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceBlanksPipe();
    expect(pipe).toBeTruthy();
  });
});
